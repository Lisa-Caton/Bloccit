 <h1><%= @post.title %></h1>
 
 <div class="row">
   <div class="col-md-8">
     <p><%= @post.body %></p>
   </div>
   <div class="col-md-4">
 <!-- #5 -->
     <%= link_to "Edit", edit_post_path(@post), class: 'btn btn-success' %>
     <!--/ edit_post_path(@post) is a helper method that is generated in routes.rb by resources :posts -->
     <!--/ which directs a user to  /posts/@post.id/edit -->
      <!--/ Run rails routes from the command line to view the post routes, if you need a refresher -->
      <%= link_to "Delete Post", @post, method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure you want to delete this post?'} %>
      <!--/  we've override the default method (:post) with :delete -->
      <!--/ We pass a Hash with the confirm: key to the data: argument. This confirms the action with a JavaScript confirmation dialog when a user presses the button. Let's watch a video exploring link_to -->
   </div>
 </div>